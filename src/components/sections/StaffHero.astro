<section class="staff-hero" aria-labelledby="staff-message">
  <img src="/images/site/staff-hero.jpg" alt="和地たなか歯科の医師とスタッフ" loading="eager" />
  <div class="staff-hero__message">
    <div class="staff-hero__content">
      <h2 id="staff-message">生きる力はお口から</h2>
      <p class="staff-hero__lead">
        食べること、話すこと、笑うこと<br />
        お口の健康は、毎日の生活を支える大切な基礎です。
      </p>
      <p class="staff-hero__body">
        私たちは、ただ歯を直すだけでなく<br />
        「一生自分の歯で健康に過ごすこと」を<br />
        サポートする歯科医院を目指しています。
      </p>
    </div>
  </div>
</section>

<style>
  .staff-hero {
    position: relative;
    height: min(66.666vw, 640px);
    overflow: hidden;
    background: #e8e9e5;
  }

  .staff-hero > img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center center;
  }

  .staff-hero__message {
    position: absolute;
    right: 0;
    bottom: 0;
    left: 0;
    min-height: 40.5%;
    padding: clamp(8px, 1.25vw, 12px) var(--gutter) clamp(14px, 1.7vw, 16px);
    background: rgba(255, 255, 255, 0.78);
  }

  .staff-hero__content {
    width: min(100%, 760px);
    margin: 0 auto;
    text-align: center;
  }

  .staff-hero__message h2 {
    margin: 0 0 2px;
    color: #008b68;
    font-family: "Yusei Magic", "Zen Maru Gothic", "Noto Sans JP", sans-serif;
    font-size: clamp(2rem, 6.4vw, 3.75rem);
    font-weight: 400;
    line-height: 1.08;
    letter-spacing: 0;
  }

  .staff-hero__lead,
  .staff-hero__body {
    color: #000;
    font-weight: 700;
    letter-spacing: 0;
  }

  .staff-hero__lead {
    margin: 0;
    font-size: clamp(0.8125rem, 1.9vw, 1.125rem);
    line-height: 1.45;
  }

  .staff-hero__body {
    margin-top: clamp(12px, 3vw, 28px);
    font-size: clamp(0.8125rem, 1.9vw, 1.125rem);
    line-height: 1.45;
  }

  @media (max-width: 767px) {
    .staff-hero {
      height: 460px;
    }

    .staff-hero > img {
      object-position: 56% top;
    }

    .staff-hero__message {
      bottom: 0;
      min-height: 49%;
      padding: 10px 18px 12px;
    }

    .staff-hero__message h2 {
      margin-bottom: 4px;
      font-size: 32px;
      line-height: 1.16;
    }

    .staff-hero__lead {
      font-size: 13px;
      line-height: 1.45;
    }

    .staff-hero__body {
      margin-top: 12px;
      font-size: 13px;
      line-height: 1.45;
    }
  }
</style>
