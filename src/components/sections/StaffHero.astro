<section class="staff-hero" aria-labelledby="staff-message">
  <img src="/images/site/staff-hero.jpg" alt="和地たなか歯科の医師とスタッフ" loading="eager" />
  <div class="staff-hero__message">
    <div class="staff-hero__content">
      <h2 id="staff-message">生きる力はお口から</h2>
      <p class="staff-hero__lead">
        食べること、話すこと、笑うこと<br />
        お口の健康は、毎日の生活を支える大切な基礎です。
      </p>
      <p class="staff-hero__body">
        私たちは、ただ歯を直すだけでなく<br />
        「一生自分の歯で健康に過ごすこと」を<br />
        サポートする歯科医院を目指しています。
      </p>
    </div>
  </div>
</section>

<style>
  .staff-hero {
    position: relative;
    height: clamp(424px, 46vw, 640px);
    overflow: hidden;
    background: #e8e9e5;
  }

  .staff-hero > img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center 26%;
  }

  .staff-hero__message {
    position: absolute;
    right: 0;
    bottom: clamp(8px, 1vw, 10px);
    left: 0;
    min-height: clamp(150px, 17vw, 169px);
    padding: clamp(9px, 0.95vw, 12px) 20px 18px;
    background: rgba(255, 255, 255, 0.82);
  }

  .staff-hero__content {
    width: min(100%, 680px);
    margin: 0 auto;
    text-align: center;
  }

  .staff-hero__message h2 {
    margin-bottom: 3px;
    color: #008b5a;
    font-family: "Hiragino Mincho ProN", "Yu Mincho", "Hiragino Mincho Pro", serif;
    font-size: clamp(2rem, 3.35vw, 2.125rem);
    font-weight: 700;
    line-height: 1.2;
    letter-spacing: 0.08em;
  }

  .staff-hero__lead,
  .staff-hero__body {
    color: #000;
    font-weight: 700;
    letter-spacing: 0.04em;
  }

  .staff-hero__lead {
    margin-bottom: 14px;
    font-size: clamp(0.78rem, 1.28vw, 0.82rem);
    line-height: 1.65;
  }

  .staff-hero__body {
    font-size: clamp(0.78rem, 1.28vw, 0.82rem);
    line-height: 1.45;
  }

  @media (max-width: 767px) {
    .staff-hero {
      height: 460px;
    }

    .staff-hero > img {
      object-position: 56% top;
    }

    .staff-hero__message {
      bottom: 0;
      min-height: 0;
      padding: 16px 16px 18px;
    }

    .staff-hero__message h2 {
      font-size: clamp(1.7rem, 8.4vw, 2.1rem);
      letter-spacing: 0.04em;
    }

    .staff-hero__lead {
      margin-bottom: 10px;
      font-size: 0.76rem;
      line-height: 1.6;
    }

    .staff-hero__body {
      font-size: 0.76rem;
      line-height: 1.6;
    }
  }
</style>
