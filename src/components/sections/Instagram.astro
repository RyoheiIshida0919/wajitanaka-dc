<section class="instagram">
  <div class="container instagram__inner reveal">
    <div>
      <span>お知らせ</span>
      <h2>最新情報はInstagramをご覧ください</h2>
    </div>
    <a href="https://www.instagram.com/wajitanakadc/" target="_blank" rel="noopener noreferrer">
      <span aria-hidden="true">◎</span>
      Instagram
      <span aria-hidden="true">→</span>
    </a>
  </div>
</section>

<style>
  .instagram {
    padding-block: clamp(60px, 7vw, 100px);
    background: var(--mist);
  }

  .instagram__inner {
    display: flex;
    max-width: 1040px;
    align-items: center;
    justify-content: space-between;
    gap: 40px;
  }

  .instagram__inner > div > span {
    display: block;
    margin-bottom: 10px;
    color: var(--green);
    font-size: 0.72rem;
    letter-spacing: 0.15em;
  }

  .instagram h2 {
    font-size: clamp(1.2rem, 2.4vw, 2rem);
    font-weight: 500;
    letter-spacing: 0.1em;
  }

  .instagram a {
    display: inline-flex;
    min-width: 230px;
    min-height: 58px;
    align-items: center;
    justify-content: space-between;
    gap: 15px;
    padding: 12px 22px;
    border: 1px solid var(--green);
    color: var(--green);
    font-family: var(--font-number);
    font-size: 0.78rem;
    transition: background-color 180ms ease, color 180ms ease;
  }

  .instagram a:hover {
    background: var(--green);
    color: #fff;
  }

  .instagram a span:first-child {
    font-size: 1.4rem;
  }

  @media (max-width: 700px) {
    .instagram__inner {
      align-items: flex-start;
      flex-direction: column;
    }

    .instagram a {
      width: 100%;
    }
  }
</style>
